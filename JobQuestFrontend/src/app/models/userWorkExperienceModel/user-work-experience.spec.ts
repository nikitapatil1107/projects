import { UserWorkExperience } from './user-work-experience';

describe('UserWorkExperience', () => {
  it('should create an instance', () => {
    expect(new UserWorkExperience()).toBeTruthy();
  });
});
